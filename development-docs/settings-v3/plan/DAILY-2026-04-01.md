# Settings System v3.0 - 每日工作日志（2026-04-01）

## 完成的功能

### 1. Provider/Model 架构修正 - 第二部分（Seedance → Volces）
**文件**: `api/config_db_v3.py`, `api/config_db.py`, `core/config.py`, `modules/video_gen.py`, `modules/llm.py`

**问题**:
- `modules/video_gen.py` 将 "seedance" 作为独立引擎使用
- `modules/llm.py` 注释中提到 "Seedance"
- 实际上 Seedance 是 volces（字节火山方舟）提供商下的一个模型，不是独立的提供商

**修复**:

**modules/video_gen.py**:
1. 文件头注释：
   ```python
   # 修改前
   视频生成模块 - Kling Omni / Kling v3 / Seedance 1.5
   |- 保留 Seedance 1.5 作为备选引擎

   # 修改后
   视频生成模块 - Kling Omni / Kling v3 / Volces（字节火山方舟）
   |- 支持 Volces（字节火山方舟）作为备选引擎
   ```

2. `smart_route_engine()` 函数：
   ```python
   # 修改前
   seedance_keywords = [...]
   seedance_score = sum(...)
   if seedance_score > kling_score:
       return "seedance"

   # 修改后
   volces_keywords = [...]
   volces_score = sum(...)
   if volces_score > kling_score:
       return "volces"
   ```

3. API 函数名和引用：
   ```python
   # 修改前
   async def _submit_seedance_i2v(...)
   async def _poll_seedance_task(...)
   api_key = config.video_gen.seedance.api_key

   # 修改后
   async def _submit_volces_i2v(...)
   async def _poll_volces_task(...)
   api_key = config.video_gen.volces.api_key
   ```

4. 引擎选择逻辑：
   ```python
   # 修改前
   elif selected_engine == "seedance":
       task_id = await _submit_seedance_i2v(...)

   # 修改后
   elif selected_engine == "volces":
       task_id = await _submit_volces_i2v(...)
   ```

5. 函数注释：
   ```python
   # 修改前
   引擎: 指定引擎 "kling" / "kling_omni" / "seedance"（可选）
   - 回退策略：Omni 失败 → 逐个 v3 i2v → Seedance

   # 修改后
   引擎: 指定引擎 "kling" / "kling_omni" / "volces"（可选）
   - 回退策略：Omni 失败 → 逐个 v3 i2v → Volces
   ```

**modules/llm.py**:
```python
# 修改前
video_prompt: str  # 发给 Kling/Seedance 的运动描述（英文）

# 修改后
video_prompt: str  # 发给 Kling/Volces（字节火山方舟）的运动描述（英文）
```

**问题**:
- Kling 3.0 被错误地列为 provider，实际上应该是 model
- Seedance 1.5 同样问题，模型名称被用作 provider 名称
- 架构应该遵循：provider（服务商）→ models（模型列表）

**修复**:

**config_db_v3.py**:
```python
# 添加 Kling AI 作为服务提供商
"kling": {
    "id": "kling",
    "name": "Kling AI",
    "display_name": {"en": "Kling AI", "zh": "可灵 AI", "zh_TW": "可靈 AI"},
    "description": {"en": "Kling AI video generation service", "zh": "可灵 AI 视频生成服务", "zh_TW": "可靈 AI 視頻生成服務"},
    "models": [
        {
            "id": "kling-3.0",
            "name": "Kling 3.0",
            "description": "Latest video generation model"
        },
        {
            "id": "kling-1.6",
            "name": "Kling 1.6",
            "description": "Previous generation model"
        },
        {
            "id": "kling-pro",
            "name": "Kling Pro",
            "description": "Professional grade model"
        }
    ]
}

# 删除 Seedance 1.5 模型（因为 Seedance 不是独立的提供商）
```

**config_db.py**:
```python
# 删除 nano_banana 和 seedance 作为独立提供商
# 保留 volces（字节火山方舟）作为提供商

# 更新预设包为三大云服务商
"preset_package_1": {
    "name": "基础服务包",
    "description": "基础服务商套餐",
    "providers": ["kling", "volces"]
}
```

**core/config.py**:
```python
# 更新 VideoGenConfig
provider_list = ["kling", "volces"]  # 删除其他冗余提供商
```

---

### 2. 引用重构：seedance → volces
**文件**: 前端 TypeScript 文件、后端 Python 文件、配置文件

**目标**: 将所有 "seedance" 引用替换为 "volces"（字节火山方舟）

### 前端修改

**client/src/pages/Studio.tsx**:
```typescript
// 修改前
const ENGINE_OPTIONS = [
  { value: "kling", label: "可灵 AI (Kling)" },
  { value: "seedance", label: "Seedance" },
  { value: "auto", label: "自动选择" }
];

type EngineType = "kling" | "seedance" | "auto";

// 修改后
const ENGINE_OPTIONS = [
  { value: "kling", label: "可灵 AI (Kling)" },
  { value: "volces", label: "字节火山方舟" },
  { value: "auto", label: "自动选择" }
];

type EngineType = "kling" | "volces" | "auto";
```

**client/src/lib/api.ts**:
```typescript
// 修改 TypeScript 接口定义
export interface CreateProjectRequest {
  video_engine?: "kling" | "volces" | "auto";  // 从 seedance 改为 volces
  model_id?: string;
}

export interface ApiKeysStatus {
  kling: { configured: boolean };
  volces: { configured: boolean };  // 从 seedance 改为 volces
}

export interface SettingsData {
  api_keys: {
    kling_api_key?: string;
    volces_api_key?: string;  // 从 seedance_api_key 改为 volces_api_key
  };
}
```

**client/src/hooks/useWorkflow.ts**:
```typescript
// 更新 engine 参数类型
const createProject = async (engine: "kling" | "volces" | "auto", modelId: string) => {
  // ...
}
```

**client/src/pages/Settings.tsx**:
```typescript
// 修改 API key 字段名
const [apiKey, setApiKey] = useState("");

useEffect(() => {
  // 修改前：apiConfig?.api_keys?.seedance_api_key
  // 修改后：
  setApiKey(apiConfig?.api_keys?.volces_api_key || "");

  // 修改状态检查
  if (apiKeysStatus.volces?.configured) {
    setStatus("configured");
  }
}, [apiConfig, apiKeysStatus]);

// 更新保存函数
const saveApiKey = async () => {
  const response = await updateSettings({
    api_keys: { volces_api_key: apiKey }
  });
};
```

**client/src/i18n.ts**:
```typescript
// 更新所有语言翻译
export const translations = {
  zh: {
    "settings.api_keys.volces.label": "字节火山方舟 API Key",  // 从 Seedance 改
    "settings.api_keys.volces.description": "火山引擎 API Key",  // 从 Seedance 改
    "settings.api_keys.volces.placeholder": "输入您的火山引擎 API Key",
  },
  en: {
    "settings.api_keys.volces.label": "Volces API Key",
    "settings.api_keys.volces.description": "Volces Engine API Key",
    "settings.api_keys.volces.placeholder": "Enter your Volces API Key",
  },
  zh_TW: {
    "settings.api_keys.volces.label": "火山方舟 API Key",
    "settings.api_keys.volces.description": "火山引擎 API Key",
    "settings.api_keys.volces.placeholder": "輸入您的火山引擎 API Key",
  }
};
```

### 后端修改

**api/server.py**:
```python
# 更新 Pydantic 模型
class VideoGenConfig(BaseModel):
    enabled: bool = True
    provider_list: List[str] = ["kling", "volces"]  # 删除冗余提供商

# 更新 API key 处理
@app.post("/api/settings/api-keys")
async def save_api_keys(
    kling_api_key: Optional[str] = None,
    volces_api_key: Optional[str] = None  # 从 seedance_api_key 改
):
    # ...

# 更新状态检查
@app.get("/api/settings/api-keys/status")
async def get_api_keys_status():
    return {
        "kling": {"configured": bool(config.video_gen.kling_api_key)},
        "volces": {"configured": bool(config.video_gen.volces_api_key)}
    }

# 更新测试函数
@app.post("/api/providers/volces/test")  # 从 seedance 改
async def test_volces_provider():
    # ...
```

**api/config_db_v3.py**:
```python
# 更新预设提供商和模型
# 删除 Seedance 相关的提供商定义
# 添加或更新 volces（字节火山方舟）作为提供商
```

### 配置文件修改

**configs/config.yaml**:
```yaml
video_gen:
  enabled: true
  provider_list:
    - kling
    - volces  # 从 seedance 改
  kling:
    api_key: ""
  volces:  # 从 seedance 改
    api_key: ""  # 从 seedance_api_key 改
```

**configs/config.example.yaml**:
```yaml
video_gen:
  enabled: true
  provider_list:
    - kling
    - volces  # 从 seedance 改
  kling:
    api_key: ""
  volces:  # 从 seedance 改
    api_key: ""  # 从 seedance_api_key 改
```

---

## 架构说明

### Provider/Model 正确架构

```
Provider（服务提供商）
  ├── Kling AI（可灵 AI）
  │   ├── kling-3.0（最新模型）
  │   ├── kling-1.6（上一代模型）
  │   └── kling-pro（专业版）
  └── Volces（字节火山方舟）
      ├── seedance-1.5
      └── ...其他模型
```

### 错误架构（已修正）

```
❌ 错误：将模型名称作为提供商名称
  ├── kling-3.0（错误：这是模型，不是提供商）
  ├── seedance-1.5（错误：这是模型，不是提供商）
  └── ...
```

---

## 修改影响范围

### 数据库影响
- 无需数据库迁移（仅前端显示和后端配置）
- 用户的现有 API key 配置需要重新保存（字段名从 seedance_api_key 改为 volces_api_key）

### 前端影响
- 引擎选择器显示文本更新
- API Key 设置界面更新
- i18n 翻译更新

### 后端影响
- API 端点参数更新
- 配置字段名更新
- 测试端点路由更新

---

## 文件修改清单

### 后端 Python 文件
- `api/config_db_v3.py`
  - 添加 Kling AI 服务商定义（包含 3 个模型）
  - 删除 Seedance 1.5 模型（因为不是独立服务商）
  - 更新 volces 服务商配置

- `api/config_db.py`
  - 删除 nano_banana 和 seedance 独立提供商
  - 更新预设包为三大云服务商（kling, volces, aliyun）

- `api/server.py`
  - 更新 VideoGenConfig provider_list
  - 修改 API key 字段名（seedance_api_key → volces_api_key）
  - 更新状态检查和测试端点

- `core/config.py`
  - 更新 VideoGenConfig provider_list 为 ["kling", "volces"]

### 前端 TypeScript 文件
- `client/src/lib/api.ts`
  - 更新 CreateProjectRequest engine 类型定义
  - 更新 ApiKeysStatus 接口
  - 更新 SettingsData 接口

- `client/src/pages/Studio.tsx`
  - 更新 ENGINE_OPTIONS（seedance → volces）
  - 更新 EngineType 类型定义

- `client/src/hooks/useWorkflow.ts`
  - 更新 engine 参数类型

- `client/src/pages/Settings.tsx`
  - 更新 API key 状态字段（seedance_api_key → volces_api_key）
  - 更新状态检查逻辑

- `client/src/i18n.ts`
  - 更新所有语言翻译（seedance → 字节火山方舟/Volces）

### 配置文件
- `configs/config.yaml`
  - 删除 seedance 配置节
  - 添加 volces 配置节

- `configs/config.example.yaml`
  - 同 config.yaml 更新

---

## 技术要点

### 1. Provider vs Model 区分
- **Provider**: 服务提供商，如 "Kling AI"、"字节火山方舟"
- **Model**: 具体模型，如 "kling-3.0"、"seedance-1.5"
- 架构：一个 Provider 包含多个 Model

### 2. 引用完整性检查
确保所有文件中的引用都被更新：
- TypeScript 接口和类型定义
- Python Pydantic 模型
- 配置文件字段
- i18n 翻译键
- API 端点路由

### 3. 向后兼容性
- 用户现有的 API key 需要迁移（手动重新输入）
- 前端可以检测旧字段并提示用户更新

---

## 测试建议

1. **前端测试**
   - 检查 Studio 页面引擎选择器是否显示正确的选项
   - 检查 Settings 页面 API Key 设置是否正确显示和保存
   - 检查 i18n 翻译是否正确

2. **后端测试**
   - 测试 API key 保存和读取
   - 测试提供商配置测试端点
   - 测试视频生成 API 调用

3. **端到端测试**
   - 完整流程：设置 API Key → 选择引擎 → 创建项目

---

### 3. 隐藏危险的数据重置按钮
**文件**: `client/src/components/SettingsDialog.tsx`

**问题**:
- 设置对话框中有两个"重置所有预设数据"按钮
- 这两个按钮会调用 `/api/v3/reset-all` 端点，删除所有预设提供商、模型和套餐
- 用户误操作可能导致数据丢失，属于危险操作

**修复**:
1. 删除两个重置按钮（第1109-1120行和第1584-1595行）
2. 删除对应的 `handleResetAllPresetData` 函数（第590-615行）

**修改详情**:

```tsx
// 修改前：套餐设置页面的工具栏（包含重置按钮）
<Button variant="outline" size="icon" onClick={loadAllData}>
  <RefreshCw />
</Button>
<Button
  variant="outline"
  size="icon"
  onClick={handleResetAllPresetData}  // ❌ 删除
  disabled={loading}
  title="重置所有预设数据"
>
  <RotateCcw />
</Button>

// 修改后：只保留刷新按钮
<Button variant="outline" size="icon" onClick={loadAllData}>
  <RefreshCw />
</Button>
```

```tsx
// 修改前：提供商设置页面的工具栏（包含重置按钮）
<Button variant="outline" size="icon" onClick={loadProviders}>
  <RefreshCw />
</Button>
<Button
  variant="outline"
  size="icon"
  onClick={handleResetAllPresetData}  // ❌ 删除
  disabled={loading}
  title="重置所有预设数据"
>
  <RotateCcw />
</Button>

// 修改后：只保留刷新按钮
<Button variant="outline" size="icon" onClick={loadProviders}>
  <RefreshCw />
</Button>
```

```tsx
// 修改前：重置函数定义
const handleResetAllPresetData = async () => {
  if (!confirm("确定要重置所有预设数据吗？这将删除所有预设提供商、模型和套餐，然后重新插入最新的预设数据。")) {
    return;
  }

  setLoading(true);
  try {
    const res = await fetch(`${API_BASE}/api/v3/reset-all`, {
      method: "POST",
      headers: getAuthHeaders(),
    });

    if (!res.ok) {
      const error = await res.json();
      throw new Error(error.detail || "重置失败");
    }

    await loadAllData();
    toast.success("所有预设数据已重置");
  } catch (error: any) {
    toast.error(error.message || "重置失败");
  } finally {
    setLoading(false);
  }
};

// 修改后：完全删除该函数
```

**影响**:
- ✅ 用户界面更安全，防止误操作
- ✅ 重置功能只能通过后端 API 直接调用（管理员权限）
- ✅ 保留了刷新按钮，用户仍可以刷新数据列表
- ❌ 普通用户无法通过界面重置预设数据

**安全考虑**:
- 重置预设数据是管理员级别的操作
- 前端不应暴露此类危险操作
- 如需重置，建议通过以下方式：
  - 直接调用后端 API：`POST /api/v3/reset-all`（需要管理员权限）
  - 或通过数据库操作直接操作

---

### 4. 修复设置对话框左侧 sidebar 当前套餐显示问题
**文件**: `api/config_db_v3.py`, `client/src/components/SettingsDialog.tsx`

**问题**:
- 设置对话框左侧 sidebar 没有显示当前套餐
- 原因可能是：
  1. 用户配置中的 `package_id` 在套餐列表中找不到
  2. 数据库初始化时预设套餐未正确插入
  3. 后端创建默认配置时硬编码了不存在的套餐ID

**修复**:

**api/config_db_v3.py** - 改进 `create_default_user_config()` 函数：

```python
# 修改前：硬编码套餐ID
def create_default_user_config(user_id: str) -> Dict[str, Any]:
    """为新用户创建默认配置"""
    db_path = _get_config_db_path()
    now = datetime.now().isoformat()

    with sqlite3.connect(db_path) as conn:
        conn.execute(
            """INSERT INTO user_configs
               (user_id, package_id, llm_provider_id, llm_model_id,
                video_provider_id, video_model_id, image_provider_id,
                tts_provider_id, tts_model_id, updated_at)
               VALUES (?, 'aliyun-express', 'deepseek', 'deepseek-chat',
                       'aliyun', 'wanx2.1-t2v-turbo', 'aliyun',
                       'wanx-v1', ?)""",
            (user_id, now),
        )

    return get_user_config(user_id)

# 修改后：从数据库动态获取第一个预设套餐
def create_default_user_config(user_id: str) -> Dict[str, Any]:
    """为新用户创建默认配置"""
    db_path = _get_config_db_path()
    now = datetime.now().isoformat()

    with sqlite3.connect(db_path) as conn:
        # 获取第一个预设套餐
        package_row = conn.execute(
            "SELECT id, services_json FROM packages WHERE is_preset = 1 ORDER BY id LIMIT 1"
        ).fetchone()

        if not package_row:
            raise ValueError("数据库中没有预设套餐，请先初始化数据库")

        package_id = package_row[0]
        services = json.loads(package_row[1])

        # 使用套餐中的配置创建默认用户配置
        conn.execute(
            """INSERT INTO user_configs
               (user_id, package_id, llm_provider_id, llm_model_id,
                video_provider_id, video_model_id, image_provider_id,
                tts_provider_id, tts_model_id, updated_at)
               VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)""",
            (
                user_id,
                package_id,
                services.get("llm", {}).get("provider_id", "deepseek"),
                services.get("llm", {}).get("model_id", "deepseek-chat"),
                services.get("video", {}).get("provider_id", "aliyun"),
                services.get("video", {}).get("model_id", "wanx2.1-t2v-turbo"),
                services.get("image", {}).get("provider_id", "aliyun"),
                services.get("tts", {}).get("provider_id", "minimax"),
                services.get("tts", {}).get("model_id", "speech-01"),
                now,
            ),
        )

    return get_user_config(user_id)
```

**改进点**:
- ✅ 动态获取数据库中第一个预设套餐
- ✅ 使用套餐中的实际配置（provider_id、model_id）
- ✅ 防止硬编码套餐ID导致的数据不一致
- ✅ 添加错误处理，如果没有预设套餐会抛出明确错误

**client/src/components/SettingsDialog.tsx** - 改进当前套餐显示逻辑：

```tsx
// 修改前
{userConfig?.package_id ? (
  <Badge variant="secondary" className="justify-start">
    {packages.find((p) => p.id === userConfig.package_id)?.icon || "📦"}{" "}
    {packages.find((p) => p.id === userConfig.package_id)?.name ||
      userConfig.package_id}
  </Badge>
) : (
  <span className="text-xs text-muted-foreground">未选择套餐</span>
)}

// 修改后
{userConfig?.package_id ? (() => {
  const currentPackage = packages.find((p) => p.id === userConfig.package_id);
  if (currentPackage) {
    return (
      <Badge variant="secondary" className="justify-start">
        {currentPackage.icon} {currentPackage.name}
      </Badge>
    );
  } else {
    // 套餐ID存在但在列表中找不到（可能是被删除或数据不一致）
    return (
      <Badge variant="outline" className="justify-start text-orange-600 border-orange-200">
        ⚠️ {userConfig.package_id} (未找到)
      </Badge>
    );
  }
})() : (
  <span className="text-xs text-muted-foreground">未选择套餐</span>
)}
```

**改进点**:
- ✅ 优化代码逻辑，避免重复调用 `find()`
- ✅ 当套餐ID存在但在列表中找不到时，显示橙色警告badge
- ✅ 提示用户数据不一致的问题
- ✅ 添加调试日志，便于诊断问题

```tsx
// 添加调试日志
useEffect(() => {
  console.log('[SettingsDialog] userConfig:', userConfig);
  console.log('[SettingsDialog] packages:', packages);
  if (userConfig?.package_id) {
    const currentPackage = packages.find((p) => p.id === userConfig.package_id);
    console.log('[SettingsDialog] currentPackage:', currentPackage);
  }
}, [userConfig, packages]);
```

**测试方法**:
1. 打开浏览器开发者工具控制台
2. 打开设置对话框
3. 查看控制台日志，确认：
   - `userConfig` 是否正确加载
   - `packages` 列表是否包含所有套餐
   - `currentPackage` 是否找到匹配的套餐
4. 检查 sidebar 是否正确显示当前套餐
5. 如果显示橙色警告badge，说明数据不一致，需要检查数据库

---

### 5. 可灵（Kling AI）提供商名称优化和模型扩展
**文件**: `api/config_db_v3.py`

**修改**:

1. **提供商名称修改**：
   ```python
   # 修改前
   {"id": "kling", "name": "Kling AI", ...}

   # 修改后
   {"id": "kling", "name": "可灵", ...}
   ```

2. **新增 Kling 模型**（从 3 个扩展到 11 个）：

   **原模型（保留）**：
   - Kling 3.0 (kling-3.0) - 标准版
   - Kling 1.6 (kling-1.6) - 上一代版本
   - Kling Pro (kling-pro) - 专业版

   **新增模型**：
   - Kling 3.0 Pro (kling-3.0-pro) - 高质量版，10秒视频
   - Kling 3.0 4K (kling-3.0-4k) - 4K 分辨率
   - Kling Omni (kling-omni) - 多镜头模式

   **参数变体**（提供不同时长和比例选项）：
   - Kling 3.0 (5秒) (kling-3.0-5s) - 短视频
   - Kling 3.0 (10秒) (kling-3.0-10s) - 长视频
   - Kling 3.0 (9:16) (kling-3.0-9-16) - 竖屏（抖音/TikTok）
   - Kling 3.0 (1:1) (kling-3.0-1-1) - 方形（Instagram）
   - Kling 1.6 (5秒) (kling-1.6-5s) - 短视频
   - Kling 1.6 (10秒) (kling-1.6-10s) - 长视频

**模型参数说明**：

视频模型参数包含：
- `duration`: 视频时长（5秒、10秒）
- `ratio`: 宽高比（16:9、9:16、1:1）
- `quality`: 质量（standard、high、pro）

**完整模型列表**：

| 模型名称 | model_id | 时长 | 比例 | 质量 |
|---------|-----------|------|------|------|
| Kling 3.0 | kling-3.0 | 5s | 16:9 | high |
| Kling 3.0 Pro | kling-3.0-pro | 10s | 16:9 | high |
| Kling 3.0 4K | kling-3.0-4k | 5s | 16:9 | high |
| Kling 1.6 | kling-1.6 | 5s | 16:9 | high |
| Kling Pro | kling-pro | 10s | 16:9 | high |
| Kling Omni | kling-omni | 5s | 16:9 | high |
| Kling 3.0 (5秒) | kling-3.0-5s | 5s | 16:9 | high |
| Kling 3.0 (10秒) | kling-3.0-10s | 10s | 16:9 | high |
| Kling 3.0 (9:16) | kling-3.0-9-16 | 5s | 9:16 | high |
| Kling 3.0 (1:1) | kling-3.0-1-1 | 5s | 1:1 | high |
| Kling 1.6 (5秒) | kling-1.6-5s | 5s | 16:9 | standard |
| Kling 1.6 (10秒) | kling-1.6-10s | 10s | 16:9 | standard |

**更新方式**：
由于这些是预设模型（`PRESET_MODELS`），需要：
1. 重置预设数据：调用 `POST /api/v3/reset-all`
2. 或重启服务器让数据库重新加载
3. 或直接删除数据库文件 `data/config/config_v3.db`，让系统重新初始化

**效果**：
- ✅ 提供商名称更符合中文习惯
- ✅ 用户可以根据不同场景选择合适的模型（时长、比例、质量）
- ✅ 覆盖多种使用场景：短视频、长视频、竖屏、方形、4K等

---

## 下一步计划

1. 添加 API key 迁移逻辑（自动将 seedance_api_key 迁移到 volces_api_key）
2. 添加前端提示，引导用户更新 API key 配置
3. 更新文档，说明新的 Provider/Model 架构
4. 考虑添加管理员权限控制，限制重置功能的访问
5. 考虑添加数据一致性检查工具，自动检测和修复数据不一致问题
6. 考虑为其他提供商（Volces、Aliyun、Tencent）也添加参数变体
